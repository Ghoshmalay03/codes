//WAP to assk the user to press 1 to find the sum of all the array elements, press 2 to find the min and max elements from the array list, press 3 to search an element using liner search, press 4 to perform bubble sort, press 5 to calculate sum of 2D matirx, press 6 to perform transpose of a matrix, press 7 to perform multiplication of two matrix.
#include<stdio.h>
int main(){
    int a, b, c, d, e, f, g, application;
    printf("Press 1 to find the sum of all the array elements \n");
    scanf("%d", &a);
    printf("Press 2 to find the minimum and maximum elements from the array list \n");
    scanf("%d", &b);
    printf("Press 3 to search the elements using linear search \n");
    scanf("%d", &c);
    printf("Press 4 to perform bubble sort \n");
    scanf("%d", &d);
    printf("Press 5 to calculate the sum of the 2D matrix \n");
    scanf("%d", &e);
    printf("Press 6 to perform the transpose of the matrix \n");
    scanf("%d", &f);
    printf("Press 7 t  perform th multipication of the two matrix \n");
    printf("%d", &g);

    switch (application)
    {
    case 0:
    printf("Enter elements for 1st matrix")
        break;
    
    default:
        break;
    }
}